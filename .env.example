# TalkEQ Environment Variables (optional)
# These are passed into the container but TalkEQ reads talkeq.conf directly.
# Use this file for any custom environment needs or future env-based config.

# TZ=America/New_York
